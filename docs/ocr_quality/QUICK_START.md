# Быстрый старт - Сбор OCR-артефактов

Краткое руководство по началу анализа чеков для построения s1_ocr_cleanup.

---

## 1. Анализ одного чека (автоматизированный)

Используйте скрипт для автоматического выявления артефактов:

```bash
python scripts/analyze_ocr_artifacts.py <receipt_id>
```

**Примеры:**
```bash
# Немецкий чек
python scripts/analyze_ocr_artifacts.py IMG_1292

# Польский чек
python scripts/analyze_ocr_artifacts.py PL_001

# Тайский чек
python scripts/analyze_ocr_artifacts.py th_TH_001
```

**Что делает скрипт:**
1. Находит `raw_ocr.json` и `ground_truth.json`
2. Сравнивает названия товаров
3. Проверяет цены, диакритику, мусор
4. Генерирует список артефактов

**Результат:**
- Вывод в консоль (краткая сводка)
- Сохранение в `data/analysis/ocr_artifacts/{receipt_id}.json`

---

## 2. Ручное валидирование

Скрипт - это помощник, но НЕ заменяет ручной анализ!

### Чек-лист валидации

Для каждого найденного артефакта:

| Артефакт | Проверки | Действие |
|-----------|-----------|-----------|
| CHAR_SUB | Действительно ли 0 → O? | Подтвердить / Отклонить |
| WORD_SPLIT | Реальный разрыв на чеке? | Открыть изображение |
| GARBAGE | Это мусор или часть текста? | Визуальная проверка |

### Шаги валидации

1. **Открыть изображение чека**
   ```
   data/input/{receipt_id}.jpeg
   ```

2. **Сравнить визуально OCR с изображением**
   - Соответствует ли артефакт?
   - Есть ли причина (шум, шрифт)?

3. **Добавить контекст в artifacts.json**
   ```json
   {
     "type": "CHAR_SUB",
     "ocr_text": "0livenöle",
     "expected_text": "Olivenöl",
     "context": "Слово в строке с ценой 4,99",
     "verified": true,  // Добавить вручную
     "fix_rule": "0 → O at word start"
   }
   ```

---

## 3. Ручной анализ (без скрипта)

Если скрипт не подходит (сложные случаи):

### Шаг 1: Откройте raw_ocr

```bash
cat data/output/IMG_1292/raw_ocr.json | jq '.full_text'
```

### Шаг 2: Откройте ground_truth

```bash
cat docs/ground_truth/001_de_DE_lidl_IMG_1292.json | jq '.items[].name'
```

### Шаг 3: Сравните вручную

Используйте таблицу в [ARTIFACT_TYPES.md](ARTIFACT_TYPES.md) для классификации.

### Шаг 4: Добавьте в artifacts.json

```bash
nano docs/ocr_quality/by_locale/de_DE/artifacts.json
```

---

## 4. Агрегация после N чеков

После анализа 10+ чеков локали:

### Шаг 1: Обновите _index.json

Автоматический расчёт можно сделать скриптом или вручную:

```json
{
  "locale": "de_DE",
  "receipts_analyzed": 10,
  "stores_covered": ["lidl", "aldi", "hit"],
  "total_artifacts": 45,
  "by_type": {
    "CHAR_SUB": { "count": 20, "percentage": 44.4 },
    "WORD_SPLIT": { "count": 10, "percentage": 22.2 }
  }
}
```

### Шаг 2: Обновите MATRIX.md

Добавьте новую строку в таблицу:

```markdown
| de_DE | 10 | 45 | CHAR_SUB (20), WORD_SPLIT (10) |
```

### Шаг 3: Формулируйте правила

Для каждого частого артефакта создайте правило:

```json
{
  "id": "de_DE_001",
  "type": "CHAR_SUB",
  "pattern": "^0(?=[a-zäöü])",
  "replacement": "O",
  "confidence": 0.95
}
```

---

## 5. Доступные чеки для анализа

### de_DE (36 чеков)

Чеки в `docs/ground_truth/`:

```bash
# Найти все немецкие чеки
ls docs/ground_truth/*de_DE*.json

# Примеры для анализа:
001_de_DE_lidl_IMG_1292
002_de_DE_aldi_IMG_1256
003_de_DE_hit_IMG_1336
```

### pl_PL (8 чеков)

```bash
# Польские чеки
ls docs/ground_truth/*pl_PL*.json

# Примеры:
010_pl_PL_carrefour_PL_001
011_pl_PL_zabka_PL_002
```

### Другие локали

- th_TH: 7 чеков (нелатинский скрипт)
- uk_UA: 1 чек (кириллица)
- bg_BG: 1 чек (кириллица)

---

## 6. Пример полного рабочего процесса

### Сценарий: Анализ немецкого чека

```bash
# 1. Запуск анализа
python scripts/analyze_ocr_artifacts.py IMG_1292

# 2. Просмотр результатов
cat data/analysis/ocr_artifacts/IMG_1292.json

# 3. Валидация
# - Открыть изображение: open data/input/IMG_1292.jpeg
# - Сравнить визуально
# - Отметить верифицированные артефакты

# 4. Добавить в artifacts.json
# Ручное редактирование docs/ocr_quality/by_locale/de_DE/artifacts.json

# 5. Обновить _index.json
# Обновить статистику

# 6. Повторить для следующего чека
python scripts/analyze_ocr_artifacts.py IMG_1256
```

---

## 7. Когда начинать реализацию s1_ocr_cleanup?

**Критерии готовности:**

- ✅ 3+ локали проанализировано
- ✅ 10+ чеков на локаль
- ✅ TOP-5 паттернов задокументированы
- ✅ Правила (fix_rules) сформулированы
- ✅ MATRIX.md заполнен

**После этого:**

1. Создать модули s1_ocr_cleanup:
   ```
   char_replacer.py   # CHAR_SUB, CHAR_MISS, CHAR_EXTRA
   word_merger.py    # WORD_SPLIT, WORD_MERGE
   garbage_filter.py  # GARBAGE
   unicode_normalizer.py # DIAC_LOSS
   ```

2. Реализовать правила из MATRIX.json
3. Написать тесты
4. Интегрировать в pipeline

---

## 8. Частые вопросы

### Q: Скрипт не нашел ground_truth?

**A:** Это нормально для некоторых чеков. Скрипт всё равно проанализирует:
- Найдет мусорные символы
- Проверит общие паттерны

Результат можно использовать как черновик.

### Q: Как отличить реальный разрыв слова от WORD_SPLIT?

**A:** Откройте изображение чека. Если слово разорвано визуально - это WORD_SPLIT. Если OCR ошибся - это CHAR_MISS.

### Q: Сколько чеков нужно проанализировать?

**A:** Минимум 10 чеков на локаль. Для de_DE (36 чеков) можно сделать выборку 15-20 самых разных магазинов.

### Q: Что делать с артефактом "UNKNOWN"?

**A:** Ручной анализ. Откройте изображение, сравните с OCR. Возможно:
- Новый тип артефакта (добавить в ARTIFACT_TYPES.md)
- Сложный случай (добавить примечание)

---

## 9. Полезные команды

```bash
# Найти все raw_ocr файлы
find data/output -name "raw_ocr.json" | head -20

# Посчитать чеки по локалям
find docs/ground_truth -name "*.json" | sed 's/.*_\(.*\)_\(.*\)_.*/\1/' | sort | uniq -c

# Статистика по artifacts.json
cat docs/ocr_quality/by_locale/de_DE/artifacts.json | jq 'length'
cat docs/ocr_quality/by_locale/de_DE/artifacts.json | jq '[.[] | .type] | group_by(.) | map({type: .[0], count: length})'

# Найти чек без ground_truth
ls data/output/ | while read d; do
  [ ! -f "docs/ground_truth/*$d*.json" ] && echo "No GT: $d"
done
```

---

## Следующие шаги

1. ✅ Выберите локаль для начала (рекомендую de_DE)
2. ✅ Проанализируйте 3-5 чеков используя скрипт
3. ✅ Валидируйте результаты вручную
4. ✅ Добавьте в artifacts.json
5. ✅ Повторите до 10 чеков
6. ✅ Перейдите к следующей локали

Удачного анализа! 🎯
